version: '3'
services:
  mchahnapi:
    image: mchahnapi #image name, Default:- Folder Name
    build:
      context: ./mchahnapi/
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
  mchahnfrontapp:
    image: mchahnfrontapp #image name, Default: - Folder Name
    build:
      context: ./mchahnfrontapp/
      dockerfile: Dockerfile
    ports:
      - "80:80"
